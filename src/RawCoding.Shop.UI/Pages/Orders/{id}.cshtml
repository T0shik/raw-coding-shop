@page
@model RawCoding.Shop.UI.Pages.Orders.OrderPage

@{
    ViewData["Title"] = $"Order {Model.Order.Id}";
    Layout = "_Layout";
}

<div class="text-white">
    <div>Name: @Model.Order.Cart.Name</div>
    <div>Email: @Model.Order.Cart.Email</div>
    <div>Phone: @Model.Order.Cart.Phone</div>

    <div>Address1: @Model.Order.Cart.Address1</div>
    <div>Address2: @Model.Order.Cart.Address2</div>
    <div>City: @Model.Order.Cart.City</div>
    <div>Country: @Model.Order.Cart.Country</div>
    <div>PostCode: @Model.Order.Cart.PostCode</div>
    <div>State: @Model.Order.Cart.State</div>

    <div>Status: @Model.Order.Status</div>

    @foreach (var p in Model.Order.Cart.Products)
    {
        <div>@p.Stock.Product.Name - @p.Stock.Description - @p.Qty</div>
    }
</div>